import * as React from "react";
import Spinner from "./Spinner";

export default {
  title: "Spinner",
  parameters: {
    component: Spinner
  }
};

export const Overview = () => (
  <div>
    <Spinner />
  </div>
);
