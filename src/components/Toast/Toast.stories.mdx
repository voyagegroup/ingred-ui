import { Meta, Story, Canvas } from "@storybook/addon-docs/blocks";
import Toast from "./Toast";
import Button from "../Button";

<Meta title="Components/Feedback/Toast" component={Toast} />

# Toast

`<Toast />` and related methods are the wrapper of [react-toast-notifications](https://github.com/jossmac/react-toast-notifications).

<!-- MEMO: Cannot display <Toast /> props type. -->
<!--       Wait for Storybook updates. -->
<!-- <ArgsTable of={Toast} /> -->

The toast is used to show alerts on top of an overlay. The toast will close itself when the close button is clicked, or after a timeout.

## Samples

Explanation of each prop is written in here's [README](https://github.com/jossmac/react-toast-notifications).

<Canvas>
  <Story
    name="Example"
    args={{}}
    parameters={{ docs: { source: { type: "code" } } }}
  >
    {() => {
      const ToastSample = () => {
        const { addToast } = Toast.useToasts();
        const handleClick = () => {
          addToast("Complete !!", {
            appearance: "success",
            autoDismiss: true,
          });
        };
        return (
          <div>
            <Button inline onClick={handleClick}>
              Show Toast
            </Button>
          </div>
        );
      };
      return (
        <Toast.Provider
          placement="top-center"
          autoDismissTimeout={3000}
          transitionDuration={300}
        >
          <ToastSample />
        </Toast.Provider>
      );
    }}
  </Story>
</Canvas>

### Colors

<Canvas>
  <Story name="info" args={{ appearance: "info", placement: "top-center" }}>
    {(args) => <Toast {...args}>{args.appearance}</Toast>}
  </Story>
  <Story
    name="success"
    args={{ appearance: "success", placement: "top-center" }}
  >
    {(args) => <Toast {...args}>{args.appearance}</Toast>}
  </Story>
  <Story
    name="warning"
    args={{ appearance: "warning", placement: "top-center" }}
  >
    {(args) => <Toast {...args}>{args.appearance}</Toast>}
  </Story>
  <Story name="error" args={{ appearance: "error", placement: "top-center" }}>
    {(args) => <Toast {...args}>{args.appearance}</Toast>}
  </Story>
</Canvas>
